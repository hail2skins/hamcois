<%= simple_form_for @page do |f| %>
  <% f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :permalink %>
    <%= f.input :content %>
    <%= f.button :submit, class: 'btn-primary' %>
  </div>
<% end %>
